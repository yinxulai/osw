# 匿名使用统计

本文定义**默认开启**的匿名使用统计：它回答「有多少人在用、用的哪个版本、在哪些地区、哪些功能真被用了」，且**不采集任何用户内容**。

隐私总原则见 [security-privacy.md](./security-privacy.md)；本文是该原则下唯一允许出站的非模型流量，其余非模型流量一律不出站。

> **状态：客户端与服务端两侧均已落地（2026-09）。**
>
> - **客户端**：core 的安装标识 / 队列 / 直连发送、管理接口的预览与上报都已回到仓库，见 §12 与 §14。
> - **服务端**：`apps/apis/`（Worker）的下游形态已定稿。转发链路是「契约 → `TelemetrySink` 抽象 → `sinks/aptabase.ts`」，端的存储是 Aptabase（欧盟区），见 §6 与 §7。
> - **共用契约**：`packages/contracts/source/telemetry.ts` 是客户端与 Worker 共用的唯一一份定义，**只描述事实，不描述去向**（§5.2）。
> - **默认开启**：`telemetryEnabled` 默认 `true`，**界面上没有任何入口**，控制台里不看、不开关、不提示（§1、§13）。
>
> 本文§1–§5 与§12–§15 描述规范与客户端行为，§6–§11 描述服务端实现。

## 1. 定位与边界

### 为什么要有它

本地工具最难回答的问题是「到底有没有人在用、用的是不是我以为的那个功能」。请求日志统计的是**用户自己的请求**，它回答不了跨用户的聚合问题。使用统计补的就是这一块。

### 与产品承诺的关系

本文与 [security-privacy.md](./security-privacy.md) 的关系在这一条上被**明确改写**：产品从「用户显式开启才上报」改为「默认上报匿名统计」——设置项 `telemetryEnabled` 默认 `true`，界面上不再有任何入口。取舍如下：

- **默认开启**：`telemetryEnabled` 默认 `true`，新装用户即产生统计流量；
- **完全不出现在界面里**：不做开关、不做说明、不做预览——采集范围不变，但它是产品自己的行为，不向用户抛出一个待办；
- **仍然可关**：字段本身保留，可由管理接口（`PUT /api/settings`）改回 `false`（即刻停止后续采集，不需要重启），也留作将来需要时的开关位；
- **可自证**（开发档能力）：管理接口的预览返回本地队列里的真实报文，与真正上报走同一条组装路径；它只在需要排查时被调用，不在界面上占位。

> 这是「默认采集、如实告知」，不是「默认同意」：所有隐私底线（不采集内容、不做设备指纹、不做用户画像、保留期可删）都不因默认开启而放宽。承诺写在 [security-privacy.md](./security-privacy.md) 里，而不是摆在每一屏上。

### 为什么要经过自建端点

统计流量不直连第三方分析服务，而是**先到我们自己的一个端点**。直接好处是密钥不落在客户端、报文可以先校验再转发；真正长期的好处是**客户端里写死的只有一个自有域名**，后端用什么、数据存哪里、存多久全都可以换，而不需要用户更新。论证与约束在 §6。

### 与出站代理的关系

使用统计**永不走用户的出站代理设置**，见 [outbound-proxy.md](./outbound-proxy.md)「非目标」：那条规则不代理遥测流量。实现上它是一个独立的直连连接器，理由有两条：

1. 用户配的代理是为**模型请求**服务的，把统计流量混进去会让代理日志里出现他没预期的目标；
2. 统计上报是「尽力而为」的流量，不该因为用户代理配置错误就永久失败，也不该在用户排查代理问题时被当成干扰项。

### 开发档不上报

`development` 环境**永远不上报**，与开关无关。开发期产生的启动次数会污染真实数据，而且开发档的数据目录（`.osw-development`）本来就是会被反复重置的。

## 2. 目标问题

首版只需回答六个问题。**这份清单就是需求边界**：不在表里的问题，首版不为其增加字段。

| # | 问题 | 观察单位 |
|---|------|---------|
| 1 | 用户趋势（活跃安装数随时间） | 去重安装 |
| 2 | 地区分布 | 去重安装 |
| 3 | 版本分布 | 去重安装 |
| 4 | 语言分布 | 去重安装 |
| 5 | 平台构成（系统 + 架构） | 去重安装 |
| 6 | 功能使用排行 | 事件次数 |


**首版只有下游一层存储，我们不自己存一份。** 「去重安装」是一个安装标识的去重计数，不是自然人的数量（见 §4）。取舍见 §8 与 §10。

## 3. 红线：绝不采集

以下内容**任何情况下都不出站**，不因开关、不因调试模式、不因错误上报而例外：

| 类别 | 具体项 |
|------|--------|
| 请求内容 | 请求体、响应体、提示词、文件内容、模型输出、SSE 帧 |
| 凭据 | API Key、Authorization、Cookie、Set-Cookie、代理账号密码、密钥环条目 |
| 供应商标识 | Provider 名称、**baseUrl / 端点地址**、供应商包内容 |
| 本机标识 | 用户名、主机名、机器 ID、MAC 地址、数据目录路径、文件名 |
| 请求标识 | `requestId`、`attemptId`、逻辑模型 ID、工作流图 ID、改写规则 ID |
| 内容规模 | Token 用量、请求耗时、错误原文——它们是**用户的业务数据**，不是产品使用统计 |
| 精确位置 | 客户端不得上报任何地理信息；地区由服务端按 Cloudflare 边缘的 `CF-IPCountry` 写入事件属性，**地址本身不转发**，见 §7 |
| 用户画像 | 不做用户级关联：报文里的标识只用来**去重计数**，下游是一个纯事件流、没有「人」这个实体，见 §7 |

供应商 baseUrl 单独强调一次：用户自建中转的地址本身就是敏感信息，泄露它是实打实的隐私事故。因此事件属性里**没有位置能随手塞进一段内容**：所有属性都是固定枚举、布尔或分桶（见 §5.3），这条红线由契约的形状保证（§5.2 的「只描述事实」原则），不是靠转发时的过滤。

**这里没有任何例外。** 首版曾为「当下什么模型热门」开过一扇小窗：启动时上报本地最常用的几个**模型名**，理由是模型名是一个看得到的产品信号。它后来被撤掉了（§2 的第七条问题随之删除），因为那扇窗的代价恰恰是本表的第一条下半行——模型名是**用户自己写下的一段字符串**，可能带着私有中转的标注或人名的后缀。一条统计信号换不来这个，而真正想要的是「哪些供应商 / 哪些协议在被用」，那是另一类可以做成闭集的问题（见 §15）。

## 4. 安装标识

### 本地标识

首次启用统计时在数据目录生成一个随机 UUID v4，写入 `telemetry-id`（权限 `0600`），与 `instance.lock`、`secrets.json` 同级。**不写进配置数据库**：它不该跟着用户配置一起被导出、备份或进供应商包。

删掉这个文件的下一个行为是「换了一台新设备」——这是正确语义，也是用户重置身份的现成手段。

### 上报的就是这个标识

上报时直接把本地 UUID 当 `installId` 发出，**不派生、不轮换**：

- 格式就是标准 UUID v4，下游可以直接拿它当「谁」用；
- **长期稳定**，所以「活跃安装数」就是它的去重计数，跨天的活跃、留存、多日累计全部现成可用；
- 服务端不持有任何可与真人对应的映射，也没有第二份标识。

**取舍是明确的：标识长期稳定，意味着下游里同一台设备的多个事件是可关联的。** 换来的是跨天分析全部现成，且客户端里少掉一整套派生逻辑。首版接受这个取舍，理由有三条：

1. 标识本身不与任何个人信息相关——没有账号、没有邮箱、没有设备指纹，删掉 `telemetry-id` 即换新身份；
2. 标识只在采集真正开启时才创建（开发档、被关闭时都不创建），而默认开启意味着正式版安装都会有它；
3. **轮换换不到真正的匿名，却会把留存报表全部废掉。** 按天轮换意味着下游里每天都出现一批「新安装」，留存、漏斗、活跃全都失去意义；而下游本来就是一个长期保留标识的系统，轮换只是把「我们手里不可关联」换成「它手里可关联但报表不可用」，净亏。

真正防「画像」的那一层不在标识上，而在**下游没有「人」这个实体**（§7）：标识是可关联的，但下游里只有事件，没有可以挂上画像的对象。这两件事要分开看——前者是可选的取舍，后者是承诺。

### 已知的取舍

同一台机器上的多个用户、多个数据目录，会被算成多个安装。本地工具没有可靠的「人」的概念，也不该为了统计去引入一个；**安装数就是安装数**，不假装它是用户数。

## 5. 事件模型

### 5.1 白名单

事件名与属性名都是**闭集**，定义在 `packages/contracts/source/telemetry.ts`，客户端、Worker、文档三处引用同一份定义。不在闭集里的事件名或属性名**在 Worker 侧被拒绝**，不是警告、不是降级，而是整批打回——这样客户端误传 `baseUrl` 之类的字段时，它在服务端进不了库。

### 5.2 信封

每条事件都带这些字段，由客户端填写：

| 字段 | 含义 | 约束 |
|------|------|------|
| `name` | 事件名 | 见 §5.3 |
| `occurredAt` | 事件发生时间（本地时钟的毫秒时间戳） | 服务端会按接收时间二次校验合理性 |
| `installId` | 本机匿名安装标识 | UUID v4 形状，不派生、不轮换 |
| `version` | 应用版本 | 由宿主注入，见 §12 |
| `os` | `win32` / `darwin` / `linux` | 枚举 |
| `arch` | `x64` / `arm64` / `ia32` | 枚举 |
| `locale` | `en` / `zh-CN` | 枚举，取用户实际界面语言，非系统语言 |
| `runtime` | `desktop` / `cli` | 枚举 |

**契约只描述事实，不描述去向。** 这是这一层唯一的设计原则，也是它叫「信封」而不是「公共字段」或「用户属性」的原因：那些词是某个后端的词汇。契约里不出现 `client_id`、`user_properties`、`params`、`event` 这类名字，也不出现「每事件最多 N 个属性」「某值最多 36 字符」这类**由某个下游的容量反推出来的数字**。写每个字段时问一句：**它在描述发生的事，还是描述它要去哪？** 后者一律不加（§6）。

违反这条原则的代价是具体的：一旦契约里出现「某个字段是用户属性」这种说法，换一个存储方案就变成一次**协议变更**——而协议是双方共用的，改它意味着客户端与 Worker 要同时升级，而已经装在用户机器上的旧版本没有第二次机会（§6）。

### 5.3 事件目录（首版）

| 事件名 | 触发时机 | 属性 |
|--------|---------|------|
| `app_started` | 服务启动完成 | — |
| `service_start_failed` | 启动最终失败 | `reason`: `instance_lock` / `port` / `database` / `other` |
| `telemetry_toggled` | 统计开关被改写（无界面入口，仅管理接口） | `enabled`: 布尔 |
| `onboarding_finished` | 引导走完或跳过 | `skipped`: 布尔 |
| `route_mode_changed` | 生效模式切换 | `mode`: `workflow` / `rules` |
| `provider_created` | 新建 Provider | `kind`: `builtin` / `custom` |
| `model_added` | 添加 ProviderModel | `protocol`: `openai-completions` / `openai-responses` / `anthropic-messages` |
| `provider_test_run` | 连接测试 | `result`: `success` / `failed` |
| `rewrite_rule_created` | 新建请求改写规则 | — |
| `protocol_conversion_used` | 一次请求发生协议转换 | `from` / `to`: 同上三值协议枚举 |
| `failover_happened` | 一次请求发生故障转移 | `attempts`: `2` / `3` / `4+` |
| `workflow_node_run` | 工作流节点执行 | `nodeKind`: 节点类型枚举 |
| `logs_exported` | 导出日志 | `withContent`: 布尔 |

约束与说明：

- 事件名与属性名只能包含字母、数字与下划线，且都以字母开头。**属性值**统一不超过 `TELEMETRY_MAX_PROPERTY_VALUE_LENGTH`（100 字符），**布尔属性用原生 JSON 布尔**（不写成 `'true'` / `'false'` 字符串——那是「下游只收字符串」留下的习惯，见 §6）。这些在 `telemetry.ts` 的类型里就卡住，不指望下游报错。
- **前缀约定。** 下游普遍把 `$` 开头的名字留给自己（保留属性、内部事件），部分系统另外保留 `_`。所以契约里的事件名与属性名不使用这两个前缀——这条约束是「不撞车」，不是「不合法」，因此它写在事件目录的注释里而不是 schema 里：**将来真的撞上了，解法是在适配器里改名，而不是改契约里的事件名**（改契约等于改历史数据的含义）。
- `protocol_conversion_used` 只发「发生了转换」与两侧协议。协议是产品能力维度（三值枚举），不是供应商标识。
- `failover_happened` 的 `attempts` 分桶，不发精确次数：精确值对产品决策没有额外信息量，却是更细的行为指纹。
- **`workflow_node_run` 的 `nodeKind` 是闭集。** 它复用 `packages/contracts/source/router/types.ts` 的 `WORKFLOW_NODE_KINDS`（一个运行期数组，不是又写一份字面量联合），所以工作流引擎新增一种节点时，统计侧要么跟着认它、要么在改契约时一起决定，不会悄悄地开始丢事件。
- **`popular_models` / `provider_model` 两个名字被撤掉了。** 它们回答的是「当下什么模型热门」，做法都是把本地最常用的几个模型名带出去，差别只在形状（前者的名次写在 `first` / `second` / `third` 三个字段里，后者一个模型一条事件、模型名写在同名属性里）。两个都从未随正式版发出，所以直接删除、不留「已停用」的占位——「只增不删」防的是历史数据失去解释，而不是防改动本身。要恢复这条信号时，正确的做法也不是把名字原样搬回来，而是重新问一次「这个答案值不值得一条用户写下的字符串」（§3）。
- 事件目录**只增不删**：删掉一个事件名等于历史数据里那一列失去解释。要停用就在文档里标「已停用」，代码里停止发送。
- **单批上限 25 条**（`TELEMETRY_MAX_EVENTS_PER_BATCH`），请求体上限 64 KiB（`TELEMETRY_MAX_REQUEST_BYTES`）。这两个数字都是**我们自己的**选择，不是下游的限额：客户端默认批量远小于它们（见 §12），它们只是「往端点灌大包」的成本下限。Worker 对超长批次直接拒绝而不是截断——截断会静默改变统计口径。

### 5.4 事件不是日志

`app_started` 之类的统计事件**不进运行日志**，也不从运行日志推导。运行日志服务于排障（见 [observability.md](./observability.md)），它有请求 ID、耗时、错误码；统计事件服务于产品决策，它必须匿名、必须低基数。两套东西的字段要求正好相反，混在一起会同时污染两边。

## 6. 上报链路与下游抽象

```
桌面端 / 命令行
    │  POST /v1/track    批量、直连、不透传用户代理
    │  报文 = 契约里的信封 + 事件（§5）
    ▼
自建 Worker（唯一持有下游凭证的一层）            apps/apis/source/index.ts
    │  1. 严格校验  2. 补服务端事实  3. 交给下游
    ▼
TelemetrySink（抽象接口，不知道下游是谁）        apps/apis/source/sink.ts
    ▼
Aptabase 批量摄取（首版唯一的下游实现）          apps/apis/source/sinks/aptabase.ts
```

### 为什么中间要有一层 sink

因为**「我们采集了什么」与「它被存到哪里」是两件独立的事**，而它们的变更频率差了一个数量级：事件目录一年改几次，分析后端可能明年就换一个。把后者写死在转发逻辑里，等于每次换后端都要动一个「已经在收数据的线上服务」。

`sink.ts` 定义的接口只有一句：

```ts
interface TelemetrySink {
  readonly name: string
  forward(events: readonly TelemetryEvent[], context: TelemetryForwardContext): Promise<TelemetryForwardOutcome>
}
```

三处刻意收窄，都是为了让它**换起来便宜**：

- **参数里没有 `Request`、没有 `env`、没有原始报文**，只有「已经校验过的事件」与两项服务端事实（服务器时钟、来源国家）。所以一个 sink 不需要懂 HTTP、也不需要懂校验——那些是 Worker 的职责。这条边界让「写一个新 sink」变成一个可以照着 Aptabase 那个文件抄的小任务。
- **返回值是三种结果而不是布尔**：`ok` / `unreachable` / `rejected` + 状态码。Worker 据此区分 502 的两种原因，日志里能看出是「下游没接」还是「下游不认」。
- **永不抛异常**。抛出去会把 Worker 变成 500（看起来像我们的 bug），而这一层能说的是「下游没接」。

于是「换后端要改哪里」有了一个可复核的答案：**只写一个新的 `sinks/*.ts`，再改 `index.ts` 里的 `createSink` 一处。** 收报文、校验、响应码这些与下游无关的代码一行都不动，它们的测试也不动——`index.test.ts` 刻意不认识下游，断言只到「发生了一次 POST、正文是 JSON」（见 §14）。

### 三条设计决定

1. **客户端只连自己的域名。** 用户能审计的网络行为是「往哪里发、发了什么」，这两件事完全由我们控制。下游的凭证只存在于 Worker 的 secret 里，不进二进制、不进设置、不进日志。
2. **端点恒定，后端可换。** 客户端里写死的只有一个自有域名，其余全在服务端：换分析后端、换存储方案、换数据驻留区域、甚至整站迁移，客户端一行都不用改。这一条比看上去重要得多——已经装在用户机器上的旧版本没有第二次机会，**发布出去的地址就是永久地址**。直连第三方分析服务做不到这一点：`google-analytics.com` 或某个厂商的采集域名一旦写进二进制，任何后端调整都只能指望用户升级，而愿意升级的用户恰恰是最不需要被照顾的那批。
3. **首版只挂一个下游，但接口不是为一个下游设计的。** 只有一个实现时最容易想到的是「把它写死」，而这里偏偏没有：`sink` 的边界、三种结果、`name` 字段，都是按「将来会有第二个」准备的。多挂一个下游时，Worker 只是多循环一次。

关于第 2 条的落地约束：

- 端点域名必须是**自有域名**，不能是平台给的子域：前者可以随时换绑、可以挂 WAF 与限流规则、可以按需切换背后的 Worker；后者绑定在平台命名空间上，改名即作废。
- 服务端对客户端承诺的是**请求格式**，不是数据去向。
- 因此客户端**不感知下游是谁**：没有项目 token、没有厂商特有字段、没有 `client_id` / `user_properties` 这些名字。`installId` 只是「本次上报的匿名安装标识」，至于它在下游被当 `sessionId` 用，是适配器的事。

## 7. Worker 的职责

三件事（见 `apps/apis/source/index.ts` 的文件头），一件都不能省：

| 职责 | 做法 | 原因 |
|------|------|------|
| 严格校验 | 用 `telemetry.ts` 的 schema 解析，拒绝多余字段 | 客户端误传敏感字段时，服务端是最后一道闸门 |
| 补服务端事实 | 服务器时钟；来源国家（`CF-IPCountry`） | 只有这一层看得到 HTTP，而这两件事客户端说不准 |
| 交给下游 | 全部转发逻辑在 `sinks/` | 职责可替换，见 §6 |

**限流不在这份清单里，这不是遗漏**：它的位置是域名，理由见 §7.1。

**没有「白名单削平」这一步了。** 契约的 `.strict()` 已经保证报文里只有契约里的字段，所以「挑认识的字段转发」是多余的；适配器直接扫描事件的键即可（§7.3）。这一条的变化本身就是 §5.2 那个原则的收益：契约足够严时，转发层不需要再筛一遍。

### 7.1 为什么 Worker 里没有限流

**限流只做在域名上**（Cloudflare 的 Rate limiting rules，配置说明在 `wrangler.toml`）。`apps/apis/source/index.ts` 里曾经有一层按安装标识与来源地址的内存计数器，删掉了，因为它做不到它看起来在做的事：

- 计数器住在**单个 isolate 的内存**里，而 Cloudflare 会同时跑很多个 isolate、很多个机房，状态既不共享也不持久；
- 于是实际放行量是「配置值 × 机房数」的量级，**一个会随部署规模自行放大的上限**——它不像一个上限，更像一个噪声源；
- 而「按客户端聚合」才是限流真正要的东西，只有边缘做得到，因为只有那里看得到同一个客户端的全部请求。

`POST /v1/track` 本身是幂等追加、无鉴权、无副作用的，漏挡不构成风险；能挡量的是那条域名规则，**而且它是这条链路上唯一的手段**——所以它不是补充项，是必需项（这也是端点必须挂自有域名而不是平台子域的原因之一）。

删掉这一层还有一个直接的隐私收益：**客户端的地址在这条链路上连读都没有被读过。** 之前它至少要被哈希一次才成为限流键；现在 `CF-Connecting-IP` 从入口到 `sinks/` 没有任何一行代码去碰它，日志里同样没有。地区那一栏走的是另一个头（`CF-IPCountry`），而 Cloudflare 在那枚头里给的是**结论**，见 §7.3。

### 7.2 只有一条路径

`POST /v1/track`，其余一律 404（`/` 也是）。刻意不给部署流水线留一个健康检查接口：这块域名上「只有一条路径」本身就是最强的信号，而部署之后真正要确认的只有「路由注册上了没有」——对 `GET /v1/track` 期待 **405** 就回答了它，那是一个只可能来自本 Worker 的答案（部署后手动 `curl -i` 一次即可）。一条只接受写入的接口没有安全的匿名读探针，与其为此开一个接口，不如让「方法不对」来证明「路径对了」。根路径也不占：它是这块域名上唯一「一看就知道该留给别的东西」的位置。

另外：

- **不做客户端鉴权。** 桌面应用内嵌的任何 token 都能被解出来，加了只提供虚假的安全感。防护靠「域名上的限流规则 + 契约的严格校验」，两者都不在客户端的掌握里。
- **不接受客户端声明的地区**：客户端报文里根本没有这个字段，请求体里若出现 `country` 会被严格校验直接打回。位置是**服务端知道的那个事实**（Cloudflare 在边缘算出的来源国家），不是客户端自述。
- **带字符集前缀的 `Content-Type` 才算 JSON。** `application/json; charset=utf-8` 接受，`text/plain` 一律 415。
- **同一批必须来自同一台设备**（`installId` / `os` / `locale` 三者一致），否则回 400 `mixed_batch`。一批在语义上就是「一个安装的一次上报」，混着发必然要把某些事件归到别的安装上去，那比拒收更糟，因为它静默地坏了口径。`version` / `arch` / `runtime` **刻意不比对**：升级与桌面/命令行切换都会让同一批里合法地出现不同的值。
- **请求格式版本化，服务端长期保留旧解析。** 端点不变意味着会有很旧的客户端一直打过来，所以新增字段必须可选，破坏性变更开新路径（`/v2/track`）并保留旧路径。这与「自有域名不可变」是同一件事的两面：既然后端可以换，老客户端就永远不会下线，那么兼容窗口就不是「支持几个版本」而是「永久」。

### 7.3 首版适配器：Aptabase

`apps/apis/source/sinks/aptabase.ts` 是**整条链路上唯一知道 Aptabase 存在的地方**（除了 `wrangler.toml` 里那一个 secret）。它做两件事：把一批事件拼成请求体，发一次 POST。

**事件在 Aptabase 里就是一个事件名 + 一堆属性 + 几项系统属性**，契约字段按这个模型各就各位——**没有对照表**，契约里的字段名直接当属性名用：

| 契约字段 | 去向 |
| --- | --- |
| `name` | `eventName` |
| `installId` | `sessionId` |
| `occurredAt` | `timestamp`（ISO 8601） |
| `version` / `os` / `locale` | 同名事件属性，**并额外镜像**到 `systemProps.appVersion` / `osName` / `locale` |
| 其余全部（`arch` / `runtime` / 各事件自己的业务属性） | 同名事件属性 |

几条值得记下的:

- **凭证在请求头里**（`App-Key`），不在 URL 里。Aptabase 的摄取端点就是这么签名的，而这也少一处「密钥会不会被记进某层访问日志」的疑问；正文里也就不必再放一份。
- **`sdkVersion` 是必填**（空字符串会让整批 400），填的是常量 `one-switch-worker`——写一个真 SDK 的名字是不诚实的，写空是不合格的。`osVersion` / `engineName` / `deviceModel` / `appBuildNumber` 一律不填：它们各有取值规范（`engineName` 指的是**浏览器引擎**），猜错只会污染下游的原生维度。
- **那三条镜像是有理由的，不是冗余。** `appVersion` / `osName` / `locale` 是下游**自己**的维度（版本报表、平台报表直接读它们），镜像一份就有现成视图，而事件属性那一份仍是分析时的权威。`osName` 尤其必须给：它为空时下游会把 User-Agent 当成一个网页事件来解析。`appVersion` 在长度超出下游的 50 字符上限时**不填**——超长的版本号不是真实场景，而一个 400 会把整批打回去，所以宁可让这一条少一个保留属性（事件属性里的 `version` 仍是原值）。
- **布尔保持布尔。** 契约里是原生布尔，适配器不提前 `String()` 它；下游要怎么规范化成字符串是下游的事。
- **一批发一次请求。** 数据模型上「一批 = 一个数组」，不拆包、不做分片。下游的硬上限是 25 条且**超了整批拒收**（不是截断），所以契约的 `TELEMETRY_MAX_EVENTS_PER_BATCH` 必须 ≤ 它——这条关系有测试钉着（`sinks/aptabase.test.ts`）。
- **区域是代码里的一个常量。** `APTABASE_EVENTS_URL` 指向**欧盟（Frankfurt）**：区域是签在 App Key 上的（`A-EU-…` / `A-US-…`），换区域就是改这一行 + 换一个 secret。两者对不上时下游回 404，所以它会在日志里立刻显形，而不是静默丢数据。选欧盟而不是美国是因为这是一条只带匿名安装标识的数据流，唯一值得挑的就是数据驻留地。

#### 匿名：下游里没有「人」这个实体

Aptabase 只存事件——没有 person profile、没有 identification、没有 feature flag。所以「不做用户画像」在这里不是一个需要去声明关掉的开关，而是**下游本来就没有这个能力**。这不是省钱技巧，而是 §4 那句「标识可关联，但我们没有把它物化成一个『人』」的最直接的执行结果。

代价说清楚：

- **得到**：按事件属性做趋势 / 漏斗 / SQL 洞察——我们全部的问题都在这类里；
- **失去**：用户属性、cohort、生命周期洞察、按用户筛选——这些概念在下游不存在，也就无所谓失去。

`sessionId` 用的是**不轮换的安装标识**（§4）：去重会话数因此等于去重安装数，跨天的活跃与留存现成可用。代价是一个安装的所有事件会串成一条无限长的会话，**不要拿它当会话时长看**：那是下游对「会话」的另一层语义，而这里它只承担「谁」这一件事。

#### 地理位置

下游的报文里**没有任何字段能收一个 IP 或一个国家**：它的托管云在边缘按请求来源 IP 判定归属。而转发请求是从 Cloudflare 机房发出的，照它去猜只会得到机房所在地。所以地区改走**事件属性 `country`**：Worker 把 Cloudflare 在边缘按真实客户端 IP 判好的 `CF-IPCountry`（一枚两字母代码）交给 sink，sink 把它落成属性。

**给的是结论，不是原料。** 用 `CF-IPCountry` 而不是把客户端 IP 转发下去，有两个后果，都是想要的：

- **客户端的地址从头到尾没有被读过。** 它既不被哈希（限流已经不在这层做，§7.1），也不被转发。报文的全部内容就是契约里的枚举值加一枚国家代码，比上一版少了一份原始用户信息；
- 于是下游自带的地区视图（根据机房 IP 判出来的那个）**必须忽略**，地区只看 `props.country`。这是这项取舍唯一要记住的地方。

拿不到时这个属性不带，而不是编一个——「不知道在哪」比「定到一个错的地方」错得少。

#### 时间戳

`occurredAt` 超出回溯窗口（或落在未来）时，适配器用**接收时间**顶上，事件照发。下游那边「一天前的不要」是它自己的规矩（而且违反时是**静默丢弃**，不报错），而这里能选的只有「时间不准」与「这条不要」：补报本来就允许损失精度（§12），所以选前者。适配器取 23 小时——比下游的一天少一小时，给时钟差留余量，这个关系有测试钉着。

### 7.4 一个必须记住的静默失败

**下游回 2xx 只说明它收下了请求，不代表事件入库。** 分批摄取这条路会**逐条**校验，不合格的条目被静默滤掉、整批仍回 200——属性名超长、必填项空值、时间超窗都可能触发；更早的那套「早于保留期的数据」也是静默丢。还有一处更绕的：**一批里没有一条合格事件时，下游会在查密钥之前就回 200**。所以：

- 这个返回值不能当验收标准用，验收要看下游自己的报表（§10）；
- 校验不能靠下游兜底——契约层（§5）才是这条链路的正确性保证，这也是为什么它的严格性不能松；
- 「密钥配错了」也不一定报错：一批全被滤掉时连 404 都不会出现，只是什么都没有。所以排查要先看**进去的是什么**，而不是先看状态码。

## 8. 数据存哪与保留期

**首版只有下游一份，我们不自己存副本。** 三个原因：

1. 六个视图下游开箱即用（§2）；
2. 自建那一层的全部价值是「回答下游答不了的问题」，而首版还没有这类问题；
3. 不存就不会丢，也不用维护一份带标识的数据。

### 8.1 保留期

事件数据的保留期是**下游项目的一个设置**（可取的值从一个月到永久），所以「能看多长的趋势」是一个可以随时改的运维决定，而不是一个要先改代码的架构决定。首版取默认值，等真的需要更长窗口时再调。

想彻底摆脱这个上限只有一个正确入口：把原始数据导出到自己的对象存储 / 数仓（下游提供这类出口）。那是「需要时再买保险」，比提前自建一套汇总便宜得多，且不需要现在做决定。

### 8.2 什么时候才加自建存储

出现下面任意一条时，加一层自有的汇总存储：

- 需要超出下游保留期的固定报表；
- 需要下游答不了的口径（自定义交叉、超大基数去重）；
- 需要「统计不依赖第三方账号」的可控性。

**加它的成本已经预付了**：客户端一行都不用改（§6 第 2 条），而 Worker 侧就是**再写一个 sink**（或者在同一批事件上多挂一个下游）——收报文、校验全都不动。这是 §6 那层抽象第一次真正派上用场的地方。

### 8.3 明确不做的事

- **不做原始事件归档**（R2 / 对象存储）。一旦需要长期分析，正确的第一步是开下游的原始数据出口；自己再存一份原始事件只是多一处带标识的长期数据，收益重复；
- **不建汇总表、不写查询脚本**：它们是 §8.2 那种需求出现之后的产物，不是现在的产物。

## 9. 端点的对外承诺

梳理一遍「谁承诺了什么」，因为这是最容易在几次重构之后变模糊的地方：

| | 承诺 | 不承诺 |
| --- | --- | --- |
| 域名 | 永久 | 背后的实现 |
| 路径 | `/v1/track` 长期保留 | 是否只有一个（将来可能有 `/v2/track`） |
| 报文 | 契约（§5）的字段与枚举 | 字段落在下游的哪里 |
| 响应 | 状态码与 `error` 取值（下表） | 具体是哪家下游、它的错误码 |

响应一览（都在 `index.ts` 里，是客户端唯一会读到的结构）：

| 状态 | `error` | 含义 | 客户端该做什么 |
| --- | --- | --- | --- |
| 204 | — | 下游已收下 | 丢弃这一批 |
| 400 | `invalid_json` / `invalid_payload` / `mixed_batch` | 报文不对 | 丢弃；`invalid_payload` 带 `issues`（前 5 条）供开发者自查 |
| 404 | `not_found` | 路径不对 | 丢弃（打错的地址） |
| 405 | `method_not_allowed` | 方法不对 | 丢弃（我们会的方法只有 POST） |
| 413 | `payload_too_large` | 超过 64 KiB | 丢弃或减小批量 |
| 415 | `unsupported_media_type` | 不是 JSON | 丢弃 |
| 500 | `not_configured` | 服务端没配下游凭证 | 丢弃并**不上报**（这是我们的问题，不该进客户端的错误提示） |
| 502 | `upstream_unreachable` / `upstream_rejected` | 下游没接 / 下游不认 | 丢弃这一批 |

**客户端对所有非 2xx 的处理都一样：丢掉这一批，不重试、不弹窗。** 上表最后一列是给排障的人看的，不是给客户端分支用的——为统计流量写一套错误处理，得到的是用户机器上的额外痕迹（§12）。

## 10. 查询与分析

首版只有一个入口：**下游自己的界面**。六个视图都在现成报表里，临时提问用它的洞察 / SQL 编辑器，不需要写代码。

**不自建看板。** 六个视图下游已经画好，而临时提问用现成界面比维护一个前端更快。真要出固定看板，那属于 §8.2 的自建存储需求——先有需求，再有界面。

**也不自建查询脚本。** 只有在开了原始数据出口（§8.1）之后，才有「本地写 SQL 查原始数据」这件事；到那时脚本与凭证再一起设计，现在写等于为一个还不存在的表设计接口。

本地开发想验证埋点是否到达下游，看它的**实时事件**列表里有没有东西，注意 §7.4 那条静默失败：回 204 不等于入库。排查顺序是「先看契约层的长度与枚举约束（`telemetry.ts`）→ 再看 Worker 报回的 `invalid_payload.issues` → 最后才怀疑下游」。

## 11. 分析口径

六个视图在下游里的取法。**全部走事件属性，没有一项需要事先注册**——这是纯事件模型带来的直接好处（§7.3 那套「用户属性 / 自定义维度」的麻烦整个消失了）：

| 视图 | 指标 | 分组依据 |
|------|------|---------|
| 用户趋势 | 去重 `sessionId` | 日期 |
| 地区分布 | 去重 `sessionId` | 事件属性 `country` |
| 版本分布 | 去重 `sessionId` | 事件属性 `version` |
| 语言分布 | 去重 `sessionId` | 事件属性 `locale` |
| 平台构成 | 去重 `sessionId` | 事件属性 `os` + `arch` |
| 功能使用排行 | 事件数 | 事件名 |

口径说明：

- **「用户数」就是去重安装数。** 下游按 `sessionId` 去重，而 `sessionId` 就是 `installId`（§4），所以它等于「这段时间内有几个安装上报过」。界面上它可能仍叫「用户」，语义上不要读成自然人。
- **版本与架构是事件属性，不是用户属性。** 所以它们读的是**那一刻的值**，而不是「最后一次的值」——「一个安装什么时候换的版本」在首版就是可回答的，这是 GA 那套用户属性模型做不到的（旧文档为此专门写过一条限制，现在没了）。
- **地区只看 `country` 属性。** 下游自带的地区维度是按转发请求的来源 IP（机房）判的，必须忽略，见 §7.3。
- **绝不把安装标识做成自定义维度**：它是超高基数。这里它走 `sessionId`，而下游的去重计数是它的本职。
- **事件计数不做去重**，它对应 §5.3 目录里每一行的触发次数。
- **跨天的留存与活跃现成可用**：`installId` 不轮换（§4）。
- **不做跨视图的交叉**（例如「某版本在某地区的人数」）：那需要 SQL 洞察。首版不做，出现需求时按 §8.2 走。

## 12. 运行时行为

### 队列与批量

- 事件先入**内存队列**，按 `批量条数`（`TELEMETRY_DEFAULT_BATCH_SIZE`）或 `间隔`（`TELEMETRY_FLUSH_INTERVAL_MILLISECONDS`）触发发送，先到先发；
- 内存队列**不落盘**：进程退出时未发出的统计事件直接丢弃。统计不是业务数据，为它写一份磁盘队列等于给用户留一份额外痕迹；
- **失败静默**：网络错误、超时、非 2xx 一律丢批次，不重试、不弹窗、不写运行日志的 `error` 级别（最多 `debug` 一行）；
- **超时短**：单次上报超时按秒级设置（`TELEMETRY_REQUEST_TIMEOUT_MILLISECONDS`），不参与任何重试预算；
- **不阻塞启动**：上报循环在监听成功后启动；`app_started` 也走同一条队列。

**统计允许丢失。** 上面这几条合起来的意思是：断网期间的启动不会被补报、闪退前的最后一个批次会消失、手动关闭统计后的那个事件可能发不出去。这不是缺陷：统计数据的用途是看趋势与量级，为它加上持久化队列、重试预算与补报机制，得到的是一份用户机器上的额外痕迹和一套跟业务数据同等复杂的可靠性要求，而这两样都不是它该有的。

**批量大小受两个上限约束。** 单批不得超过 §5.3 的 25 条，请求体不得超过 64 KiB。这两个数字都在契约里（客户端能在发之前自查，两端永远是同一个值）。它们不是性能调优，而是「小批量、允许多发几次」这个取向的边界。

### 版本号来源

core 是纯 Node 进程，拿不到渲染层的 `__APP_VERSION__`，也没有 `electron`。因此**版本号由宿主注入**，随启动参数一起传进 core，core 不自己解析版本、不读 `package.json`：

- 桌面形态：主进程用 `app.getVersion()`；
- 命令行形态：cli 宿主自行解析。

### 采集与保留

- 关闭采集**只影响后续采集**，不删除已经上报的数据。下游侧要按标识删数据是一个人工流程，而用户能拿出的凭据就是他的 `installId`（管理接口的预览里能看到它）——这也让「我的数据」有一个可指认的对应物；
- 本地只有 `telemetry-id` 一个文件，删掉即等于换新设备。旧标识在下游里的记录不再产生新事件，过了保留期自然过期。

## 13. 设置与说明

`SettingsSchema` 保留两个字段：

| 字段 | 类型 | 默认 | 语义 |
|------|------|------|------|
| `telemetryEnabled` | boolean | `true` | 是否上报匿名使用统计。默认开启，界面上不提供开关 |
| `telemetryEndpoint` | string | 空 | 保留字段，仅开发档覆盖，见下 |

**为什么保留 `telemetryEnabled` 却不给开关。** 界面上没有入口，是因为「是否上报」在产品里是既定行为，不该让每个用户替产品做一次决定；字段本身仍然保留，是因为它是「采集是否被允许」的唯一判据，也是唯一能立刻停掉流量、且不需要发版的开关位（管理接口 `PUT /api/settings` 可写入）。默认 `true`。

**关于端点是否可配。** 端点写死在 `telemetry.ts` 的常量里，**不暴露给用户设置**：一个「改上报地址」的输入框既是无用的自由度（用户没有别的接收端），又是一个把统计流量导向他处的口子。`telemetryEndpoint` 仅作为开发档覆盖（指向本地 Worker），正式构建下为空即用常量。

设置页**不做任何展示**：没有开关、没有说明、没有预览区。

- 统计默认开启是产品既定行为，不是一项要用户拍板的设置；多一个控件（无论开关还是提示）就是多一次「请用户替产品做决定」，而这正是本次改写要拿掉的东西；
- 不展示不等于不告知：口径写在 [security-privacy.md](./security-privacy.md) 与本文里，是产品承诺的一部分；需要自查时走管理接口的预览，而不是在设置页占一个位置。

**若将来需要关闭**，没有界面入口，但字段仍在：通过管理接口写入 `telemetryEnabled: false` 后，采集会**尽力补发一条 `telemetry_toggled`**。关掉之后就不再采集，所以这一次是最后一次机会；能发出去就发，发不出去就丢。没有它，我们只能看到「某个安装从某天起不再出现」，而这既可能是关了统计，也可能是卸载或断网——区分不了就等于没这条数据。

界面文案（若有）会进 UI 语言目录，与 [i18n.md](./i18n.md) 的三层模型一致。

## 14. 实现落点

| 落点 | 内容 |
|------|------|
| `packages/contracts/source/telemetry.ts` | **契约层**：信封 schema、事件名闭集、属性枚举与长度约束、批量与体积上限、端点常量。客户端与 Worker 共用的唯一一份定义；**只描述事实，不描述去向**（§5.2） |
| `apps/apis/source/sink.ts` | **下游抽象**：`TelemetrySink` 接口、三种转发结果、`postJson`（带超时与额外的请求头）、`Fetcher` 类型 |
| `apps/apis/source/sinks/aptabase.ts` | 首版唯一的适配器：字段去向、保留属性镜像、`country` 属性、区域常量与批量上限 |
| `apps/apis/source/index.ts` | Worker：路由、配置检查、严格校验、同设备检查、把结果映射成状态码与一行日志；把 `CF-IPCountry` 转成 `countryCode` 交给 sink |
| `apps/apis/source/index.test.ts` | Worker 的行为测试。**刻意不认识下游**：只断言「一次 POST、JSON 正文」，以及状态码与日志 |
| `apps/apis/source/sinks/aptabase.test.ts` | 适配器的测试：字段去向、保留属性镜像与长度降级、`country`、时间戳窗口与降级、三种结果、超时 |
| `apps/apis/wrangler.toml` | 域名绑定（自有域，见 §6）、`[observability]`、**限流规则的位置**（§7.1）、**secret 说明**（`APTABASE_APP_KEY`，含区域提示） |
| 下游控制台（不在仓库里） | 建项目、取 App Key、按需调整保留期（§8.1） |

### 客户端侧

| 落点 | 内容 |
|------|------|
| `packages/contracts/source/schemas.ts` | `telemetryEnabled`（默认 `true`，无界面开关）/ `telemetryEndpoint`（默认空串，仅开发期覆盖）设置项 |
| `packages/core/source/telemetry/install-id.ts` | 安装标识的读写与落盘；只在采集真正开启时才创建 |
| `packages/core/source/telemetry/queue.ts` | 内存队列：容量上限、批量阈值、定时冲刷、`unref()` 定时器 |
| `packages/core/source/telemetry/sender.ts` | 直连发送：`createOutboundConnector(直连配置)` + `createCoreNetworkClient()`，非 2xx 视为失败 |
| `packages/core/source/telemetry/index.ts` | 生命周期：开发档短路、开关订阅、补信封字段、`app_started` / `telemetry_toggled`、启动失败直发、预览 |
| `packages/core/source/database/analytics-store.ts` | 控制台图表的取数：延迟分布、模型统计等；**不参与上报**（§3 不再有任何自由文本字段，上报侧也不需要读本地库） |
| `packages/core/source/runtime/server-runtime.ts` | 启动上报循环、停机清理、启动失败原因分类 |
| `packages/core/source/management/routes/operations/telemetry.ts` | 预览接口 `/api/telemetry/preview` 与静默接受接口 `/api/telemetry/report` |
| `packages/console/source/api/runtime.ts` | `telemetryApi.report`（控制台只负责把界面侧的事实转交给 core；`/telemetry/preview` 仅供 curl 自查，控制台不再包装它） |
| `packages/console/source/pages/runtime-settings/page.tsx` | 设置页**不涉及统计**：既不渲染开关也不渲染说明 |

网络层的先例是 `packages/core/source/management/routes/diagnostics/outbound-proxy-test.ts`：`createOutboundConnector(直连配置)` + `createCoreNetworkClient(connector)`，用完 `destroy()`。**不要动 `coreNetworkClient` 单例**，它承载的是用户代理语义。

端点域名是 `telemetry.ts` 里的一个构建时常量，构建时就是最终值。改动它的门槛比看上去高得多：**已经发出去的客户端里的地址改不了了**（见 §6），所以上线前就该把它当作永久前缀来选。

`apps/apis/` 与客户端**没有版本耦合**：Worker 可以随时改校验规则、换后端、换存储、加汇总任务，不需要等客户端发版；反向不成立，所以 Worker 侧的每一次改动都要按「对面是最老的版本」来验证。

## 15. 非目标

- 首版不自己存一份统计：不建原始层、不建汇总表、不做归档（§8.3），只保留下游一份；
- 不自建看板与查询脚本（§10）；
- 不做崩溃与异常上报：它的脱敏要求、保留期、采样策略与使用统计完全不同，混在一起会同时破坏两边（要做得单独设计）；
- 不上报请求级指标（耗时、Token、状态码）：那是用户自己的业务数据，已在本地观测里完整存在；
- 不做 A/B 实验与远程开关（下游是一个只收事件的纯分析服务，本来就没有这个能力；而这类事情也不该由统计链路来做）；
- 不采集任何自由文本；
- 不做用户画像：下游里没有「人」这个实体（§7.3）；
- 不因统计失败影响任何用户可见行为。
